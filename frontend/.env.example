# Backend server used by the Next.js route handlers
BACKEND_API_BASE_URL=http://localhost:5000

# Optional public base URL for the frontend API gateway
# Leave blank to use the same origin as the Next.js app
NEXT_PUBLIC_API_BASE_URL=
